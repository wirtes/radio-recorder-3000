services:
  radio-recorder:
    build: .
    restart: unless-stopped
    ports:
      - "8585:8585"
    environment:
      TZ: America/Denver
      DATA_DIR: /data
      FINAL_DIR: /server-share
    volumes:
      - ./data:/data
      - ./Music/server-share:/server-share
